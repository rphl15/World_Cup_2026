
```{r}
install.packages("DBI")
install.packages("RMySQL")  # ou RMariaDB (mais moderno)
```


```{r}
library(DBI)

con <- dbConnect(
  RMySQL::MySQL(),
  host = "localhost",
  user = "root",
  password = "SENHA",
  dbname = "World_Cup_26"
)
```

```{r}
dados <- dbGetQuery(con, "SELECT * FROM estadios")
print(dados)
```


```{r}
```


```{r}
```

